# 2026-09-06

## GroupViz 对接待办核验（用户已处理，全绿）
- P0-1 门面导出 descriptor ✅：src/core/index.ts:12 已加 `export * from './descriptor'`。
- P0-2 越界引用清零 ✅：grep `../utils`/`../components` 在 src/core 下 0 匹配；notationParser/importGroup 已改 core 内部引用，groupFactory 移入 core（index.ts:29 导出 ./groups/groupFactory）。
- P1 binomialMod ✅：新增 algebra/combinatorics.ts，Lucas 定理 + 模幂 + 逆阶乘实现，index.ts:35 导出。
- GroupDescriptor v1 完整字段：schemaVersion('1')/symbol/order/elements[{id,label?,order?}]/multiply([row][col]=elementIndex)/可选 name/properties/construction/source。serializeDescriptor 输入为 {symbol,name?,order,elements,multiplyTable?,properties?,construction?,source?}。

## 关键架构发现（决定 M0 渲染路线）
- GroupViz React 视图组件在 src/components/（Canvas/Panels/TestPage 等），与 app 强耦合（依赖 context/hooks/theme/i18n），**无独立 @groupviz/react 包、无干净入口**。
- 但 core 内有**纯算法布局层**：cayleyEdges/cayleyTree/forceLayout/cycleLayouts/latticeLayout/layout3D/shapeLayouts，输出坐标不碰 React。
- **决策：Galculator 渲染层自研**（core 布局算法 + 自写 SVG），不等 @groupviz/react 视图包。理由：①视图包 2026 底才发，等不起；②证明可见化（轨道分解/陪集/高亮）本就是差异化，GroupViz 视图也画不了；③简约交换图风是 Galculator 自己的设计目标，自研可控。

## M0 方案（纯前端，先不起后端）
- 步骤：初始化 Vite+React19+TS → vite alias @groupviz/core→GroupViz src/core → core 建 A₄（createAlternatingGroup(4) 或 createGroupFromSymbol('A4')）→ serializeDescriptor→GroupDescriptorV1 → core 布局算坐标 → 自写 SVG 画 Cayley 图。
- GAP 后端留到 M1/M2（证明步骤需轨道分解/稳定子等大群计算时再接入）。
