# 2026-09-16

## 决策定案：第 5 条（固化集合是否自动升级）→ 选 A
- 用户答「A」：`{x ∈ A₄ : x²=e}` 这类固化结果即使恰好是子群（V₄）也**仍是 subset 对象（圆）**，系统只**提示**「这其实是个子群，要不要升级成群对象？」，由用户点一下升级。
- 理由（已写进 INTERACTION v2 §6.4）：**对象类型是用户的心智模型，系统悄悄改会造成"这到底是集合还是群"的混乱**；但升级入口必须在手边，不藏进菜单。
- 至此 5 条待拍板全部定案（另 4 条采纳我的建议，见 INTERACTION §13.1）。

## 文档收敛（v1/UI_PLAN/DESIGN 合并）
- **INITIAL 诊断**：`UI_PLAN.md`(298 行) 与 `INTERACTION.md`(238 行) 大面积重叠；`DESIGN.md`(71 行) 八成本身是指针（"详见 ARCHITECTURE / INTERACTION"），造成文档间来回跳。
- **动作**：
  - **`INTERACTION.md` 升 v2**（393 行）：吸收 UI_PLAN，13 节。新增/重写：§0 一句话理念（用户应在对象旁边完成操作）· §3 输入层（三种形态 + 两块输入框 + 自动命名避开调用名 + `resolveElement` 支持循环记号）· **§4 操作入口：三个入口 = 一张表**（`opsFor(selection)` + 交互状态机 + 径向菜单三类两层 + 顶部工具条）· §5 左栏两态 · **§6 集合构造器**（FilterSpec / 分面表格 / 类型×属性第二重身份 / 属性谓词 vs 字谓词 / 固化对象）· §7 操作 = 结果对象 + 结构伴生 + 三种边 · §8 布局（自动/手动 + 钉住语义）· **§11 CanvasGraph 与代码对齐**（v1 里那份 `CanvasNode{kind,collapse,ref}` 是过时的，改成真实的 `GalObject` + `CanvasNode{shape,level}` + `GalEdge{map|action|provenance}`）· §13 五条决策 + 遗留。
  - **`DESIGN.md` 拆解**：§1 产品定位 + 三档参照 → README「定位」（补了 Desmos/Group Explorer/Lean 三档，明确 Lean **不碰**）；§2.1 引擎依赖 → `ARCHITECTURE.md` §11（连"`@groupviz/react` 单一 bundle 顶层 import three/r3f/drei"这条坑一起）；§4 后端方针 → `ARCHITECTURE.md` §7；§3 契约 → `ARCHITECTURE.md` §12 契约索引（GroupDescriptor v1 / Proof Spec / FilterSpec）。
  - **`ARCHITECTURE.md` 补强**：§6.1 对象编辑器补 core 落点（`getGeneratorElements` → `extendFromGenerators` → `verifyHomomorphism` 的 `violation{a,b,lhs,rhs}`，结论"引擎侧已完整，缺的只是一张表单"）；§6.2 搭积木改为指向 INTERACTION §6 + FilterSpec；§10 删掉已定的"搭积木 UI 细节"；新增 §11 引擎依赖、§12 契约索引。320 行。
  - **归档**：`docs/archive/` 收 `DESIGN.md` / `UI_PLAN.md` / `GROUPVIZ_HANDOFF.md`，并写 `docs/archive/README.md` **登记每份文档的去向** + 三条归档规则（只移动不删除 / 归档前确认内容已被吸收 / 头部加归档标注）。三份文档头部都加了归档说明，相对链接批量上跳一级（6 处）。
  - **README**：文档导航改成表格（ARCHITECTURE / INTERACTION v2 / PROOF_SPEC / ROADMAP + archive），项目状态补「规划已收敛 + 下一步 U0」。
- **链接全绿**：写脚本扫了 README + docs/*.md + docs/archive/*.md 的全部相对 md 链接，修完 0 BROKEN。

## 初始化 git 仓库（用户要求）
- 项目原本**不是 git 仓库** → 删除/移动不可回滚。用户一句话：「非git仓库？那你git一下」。
- `git init -b main`；身份沿用全局（cathylinlin / bluejam001@163.com）。
- **3 个提交**：`d06265b` 初始（40 文件，v0.0.0 状态）· `6d2ab00` 加 `.gitattributes` · `a8de469` 修归档链接。工作区干净，`.git` 480K。
- **排除规则**照旧生效：`node_modules/` `dist/` `.tmp-*`（含 `.tmp-npm/`）。纳入的 40 个文件里含 `docs/assets/*.png`（3 张里程碑截图）与 `.workbuddy/memory/*`（项目记忆）。
- **`pnpm-lock.yaml` 已纳入**（库项目应当纳入）。
- **`.gitattributes` 新增**：`* text=auto eol=lf` + 二进制例外。理由：仓库本来已存 LF，但 Windows 上 `git checkout` 会把 LF 翻成 CRLF，之后每次 `git add` 都静默重新规范化 → 整文件 diff 噪音。实测仓库 blob 与工作区字节数一致（均 4751），确认是 LF 存储，所以加 `eol=lf` 不会产生 renormalize 变更（提交后 `git status` 确实只有新增的 `.gitattributes`）。
- **未做**：没有加 remote、没有 push（对外动作，先问）。

## 待办
- 无 remote。若要备份/协作需用户给仓库地址（本机 `github.com` 走 `~/.ssh/config` 的 ssh.github.com:443 别名，普通 `git push` 可直接用）。
- 下一步 U0（见下，当日已完成）。

## U0 落地：参数类型 + opsFor + 集合运算 + 子群升级
- **注册表 20 条 → 27 条**，每条补齐 `params: OpParam[]`（`ParamType` 7 类：group / subset / action / map / element / prime / int），模块加载即断言 `params.length === arity + optional` 且**标量参数只能在末尾**（`opsFor` 依赖前缀匹配）。
- **`opsFor(selection)`** 三条规则：① 选中值逐参前缀匹配；② 剩余必需参数必须是标量（能由输入框补，不算"还缺一个对象"）；③ 需再选一个对象的操作不出现。`subset` 对**群节点**的放宽是有条件的——前面有群参数时要求"是它的子群"（`isSubgroupElementSet`），否则选中两个群会冒出多余商群候选。
- **新增 7 条**：`∩` `∪` `\` `·`（同母群校验；积集走母群 `multiply`；产出 `elements`，**不自动升级**＝决策⑤）+ `⟨S⟩` 闭包（`closeUnderMultiply` → 真群对象；也吃 `闭包(G, r2)` 上下文群形态）+ `ker` / `im`（吃 `map` 值 + `GalMap.mapping` 完整映射表，`value.ts` 已补该字段）。
- **子群升级为真群对象**：center / centralizer / normalizer / commutatorGroup / closure / kernel / image 全改走 `buildSubgroupGroup` → `result: 'group'`。画布**圆变方**，`Z(Z(G))` 合法；副行带结构符号（`|Z| = 2 · C₂`，`subgroupStructureSymbol` 识别不出时不留脏尾巴）。
- **元素参数一律走 core `resolveElement`**（`ord(S_4, (123))` 合法），`omegaIndexOf` 同步改；`findElement` 手写版删除。
- **输入层**：`normalizeExpr` 新增 `⟨X⟩` → `闭包(X)`（**只改无逗号形态**，`⟨(12),(34)⟩` 母群未定，留给记号解析）、`∖`、`·`、`\cdot`；`findTopLevelInfix` 让 `·` 与 `/` 一样无需词边界（`A·B` 可写）。
- **UI 第一个入口**：左栏「可用操作」在选中对象后切成"可用于「X」· 可用 N 条"，可一键切回全部 27 条。App 默认行改为展示 U0 能力；详情面板 `group` 分支补元素列表（升级后 Z(G) 走 group 分支，否则看不到元素）。
- **真跑验证**：vite SSR 打包 + node 直跑 **52/52 断言**（`apps/web/.tmp-verify/u0.ts`：集合运算、闭包阶、`Z(Z(G))`、opsFor 匹配表、ker/im、注册表自洽）；playwright 真浏览器控制台**零错误**、6 节点 SVG（4 rect + 2 circle）正常；截图存 `docs/assets/u0-ops-for.png`。
- **踩坑**：① 闭包 `⟨J⟩` 首参在集合模式下被当成元素参数重复解析 → 元素参数循环一律从 1 号位起；② 我自己拿 `r2`（阶 2）当阶 4 断言，是断言错不是代码错（D₄ 的 label 是 `e r r2 r3 s sr1 sr2 sr3`）；③ Bash 里手写工程目录易把 Galculator 拼错 → 改用 `cd /c/newproject/Gal*/apps/web` 通配符。
- **文档同步**：INTERACTION §2 / §3.3 / §4.1 / §13.2 · ARCHITECTURE **新增 §3.5 子群的类型升级** + §5.2 / §5.4 · ROADMAP（U0 小节 + 表格 ✅）· README（状态 + 操作表 + 截图）。
- **下一步 U1**：输入框拆「名字 / 表达式」两栏 + 自动命名（避让注册表 `call` 名——注册表已具备）。

## U1 落地：两块输入框 + 自动命名 + 边打边校验
- **新增 `apps/web/src/gal/naming.ts`**：保留名清单**从注册表自动导出**（`OPS` 的 83 个 `call` 名）+ 惯例单字母 `e`/`p`/`g`/`h`；`nextAutoName`（`A B D F I J K L M N O Q R S T U V W X Y` → 20 个单字母用尽后进 `A₁ B₁ …`，跳过已用名与保留名，大小写不敏感）、`checkName`（非法字符 / 重名**拦**；命中操作名**只提醒**）、`autoNamePreview`、`isNameLike`。
- **输入框拆两块**（`InputPanel`）：「名字(可空) | = | 表达式 | 添加」。名字 `placeholder` 直接显示**将分配的名字**（`title` 也给）。
- **边打边校验**：复用 `evalExpr`（预览与提交结果必然一致）——合法显示 `✓ [类型chip] 标签 · 副行`，非法红字 + core 定向提示（含可用写法清单），非法时「添加」置灰。`aria-invalid` 驱动红框。
- **名字提醒与表达式预览并存两行**（首版把 warn 放在预览之后被挤掉了，实测第④态发现 → 改成 warn 独立一行）。
- **兼容整行粘贴**：名字栏空 + 表达式栏贴 `G = D_4` → 自动拆分；左半边不像名字（如 `x^2`）时整体当表达式。
- 快捷键 `Enter` 提交 / `Esc` 清空两栏。
- **验证**：tsc 零错误；**U1 35/35 断言**（含强性质：连取 60 个自动名全部避开保留名且互不重复；自动命名出的定义行真能建对象、老写法照常；`手写 Z 后 Z(Z) 仍走注册表`）+ U0 **52/52 回归**；playwright 真浏览器零错误，五态截图存 `docs/assets/u1-composer.png`。
- **踩坑**：①`pretty.ts` 的 `subscript` 收 `string`，传 `number` 三处 tsc 报错；②`node script.mjs | head -N` 会因 EPIPE 让整条命令异常退出且**无输出** → 改 `> log 2>&1` 落盘再读；③断言又错一次（自动名跳号后我按 `A..J` 推 `K`，实际是 `N`）→ 改成自洽断言（`nextAutoName(seq.slice(0,9)) === seq[9]`）。
- **文档同步**：INTERACTION §3.2 重写成落地口径 · ROADMAP 加 U1 小节 + 表格 ✅（顺带把"UI 重构线 U0–U6"标题改成 U0–U7）· README（快速开始 / 状态 / 项目结构 / 双截图）。
- **下一步 U2**：画布交互状态机 + 径向菜单（看/算/造三类）+ 左栏详情态（竖卡）。

## 推送上线（用户「先推吧」）
- **远端 `rrCathy/Galculator`（public）**：MCP 的 GitHub token **没有建仓权限**（`create_repository` → `403 Resource not accessible by integration`），改由用户在网页建**空库**（已提醒不要勾 README / .gitignore / license，避免初始 commit 撞本地历史）。
- `git remote add origin git@github.com:rrCathy/Galculator.git` → `git push -u origin main` 成功。**默认沙箱会拦**（`git push` 要读 `~/.ssh` → `Host key verification failed` + `SANDBOX EXECUTION REJECTED`），必须 `dangerouslyDisableSandbox` 走一次授权；而 `ssh -T git@github.com` 单独跑能过（走 escalation），别拿它当 push 也能过的证据。
- 远端 HEAD `4a3a279…` 与本地 `git rev-parse HEAD` 一致；`main` 已跟踪 `origin/main`。
- **提交只有一个**（不是 U0/U1 各一个）：两者改动咬合（InputPanel 同时承载两阶段、`naming.ts` 被 U0 之后的 InputPanel 直接 import），拆开会得到编译不过的中间提交。范围：15 文件，+1265 / −135，含新增 `apps/web/src/gal/naming.ts` 与两张截图。

## U2 落地：在对象旁边就能操作
- **新增 4 个模块**：`gal/compose.ts`（操作+实参 → 定义行；函数头挑合法 call 名，`⟨⟩` 不能当函数名）· `gal/interaction.ts`（`Interaction` 状态机 + 算/造二分 + `canPick` + `pendingHint`）· `ui/RadialMenu.tsx`（三类两层环绕菜单，短标签 + 边界钳制）· `ui/Inspector.tsx`（竖卡）。
- **状态机多出一档 `fill`**：原方案只有 `pending`（等对象参数），但 `pSub(G,p)` / `ord(G,g)` 的末尾参数是**标量**，画布上点不出来 → 加"补参条"（素数默认 2、元素记号留空待填，回车执行）。
- **执行路径统一**：点出来的操作先**编回一行文本**再交给 `evalExpr` —— "点出来的"与"打出来的"行为必然一致，且左栏真的多出一行可读可改。同名同标签重复计算**去重**（选中已有对象）。
- **pending 四件反馈**：顶部提示条 / 十字光标（`.canvas.picking`）/ 已选节点换色加粗 / **不可点节点变暗**（最后一条是加的：点击失败的挫败感比高亮带来的便利更大）。
- **左栏两态**：点节点即切详情态（竖卡），底部详情条整体搬进左栏并删除；竖卡分区按值类型变（group 有 基本信息/识别/元素表/共轭类/**子群按共轭轨道合并**）。
- **真浏览器抓到的核心 bug**：径向菜单第一层「造」**恒为空** —— 我误用 `opsFor` 去筛多元操作，而 `opsFor` 语义是"选中值能把参数填满"，单选一个对象时多元操作根本不在结果里。改成直接遍历 `OPS` + `paramAccepts(params[0], v, [])`。**这条只有真跑 UI 才能发现**（单元断言当时还"PASS 了一个群的「造」是空的"——断言写错了方向）。
- 另一个小 bug：竖卡里 `listCosetStripSubgroups` 的 `structure` 是 **TeX**，漏了 `prettySymbol`，显示成 `C_{2}\times C_{2}`。
- **验证**：tsc 零错误；**U2 72/72 断言**（compose / 闭环 14 条"组装出来的行真能求值" / 算造二分 / canPick / 状态机）+ U0 52/52 + U1 35/35 回归；playwright 十条路径走查零错误，截图 3 张入 `docs/assets/u2-*.png`。
- 文档同步：INTERACTION §4.2/§4.3/§5 全部改成落地口径（含"造类不能用 opsFor 筛"这条坑）· ROADMAP 加 U2 小节 + U3 预告 · README（快速开始/状态/结构/双截图）。
- **下一步 U3**：映射构建器（生成元填像）→ 第一条实线箭头；`ker`/`im` 自动可用。

## UI v3 改版（用户要求 → U2.5 落地）
- **布局根本变化**：从"左侧 320px 侧栏"改成**画布铺满 + 四个浮层抽屉**（左上并排「对象」（含两块输入框）/「操作」（运算产生的对象）/「信息」，左下「数值」**上拉**抽屉）。默认只展开对象+数值 —— 全摊开会盖住画布左上角，连顶部球都压上去。**「可用操作清单」面板取消**（用户定：操作区 = 运算产生的对象）。
- **画布给抽屉让位**：抽屉的容器像素（left/bottom）用 ResizeObserver 量出来交给 `CanvasView` 的 `insets`，内容在剩余区重新居中缩放 —— 不做这步节点会整片钻到抽屉底下。代价：抽屉开得越多内容缩得越小（必然取舍）。
- **对象悬浮球**（`ui/ObjectOrb.tsx`）：节点**左上角**深色球 → 环绕 4 按钮（基本 / 元素 / 子群 / 操作）。前三个切信息面板 tab；第四个是**单对象操作下拉面板，点一下即建对象**。
  - 环绕角度**只铺上半圈 + 左侧**（-180°→0°）：球在节点左上、节点在球的右下，整圈均分的话正下方那颗会结结实实压住节点标签。
  - 半径随节点尺寸放大（`max(54, r+22)`）；球与卫星都做容器边界钳制。
- **多对象球**（`ui/MultiOrb.tsx`）：显示区正上方，列**全局**多对象操作（8 条），选中后空着手进 pending。位置 `left: max(50%, 面板右边界+70px)` 避让抽屉（否则被盖住 —— 走查抓到的）。
- **元素横滚表格**（`ui/ElementsTable.tsx` + `gal/summary.ts`）：**列 = 元素、行 = 属性**（阶 / ∈Z? / 共轭类 / 类大小 / 中心化子），行首列 sticky、横向滚动。
- **数值区**（`gal/numeric.ts`）：只放两种 —— **拖动收集**（dragstart 写 `application/x-gal-number`，整块面板体即投放区、拖上去有虚线高亮）+ 主动计算的数值；键前缀 `d:` / `c:` 区分来源。
- **KaTeX 接上**（`ui/Tex.tsx` + `main.tsx` 引 CSS）：面板里的 TeX 真渲染（子群结构符号等，实测 6 个 `.katex` 节点）。
  **画布节点标签仍用 Unicode 近似**——理由是 SVG 里放 KaTeX 要 foreignObject + 渲染后二次测量，且节点标签是混合语义展示串（`Z ∩ C`、`⟨r⟩`、`Sub(D₄)`）不是 TeX 源。写进 INTERACTION §13.3。
- **删除**：`InputPanel.tsx` / `Inspector.tsx` / `RadialMenu.tsx`（被取代）。
- **API 变动**：`interaction.ts` 的 `splitForNode` → `singleOpsFor(v)`（`objectArity ≤ 1` 且**排除产数值**——用户说"计算先不弄"）+ `multiOps()`（全局）。
- **验证**：tsc 0；U2 断言 **76/76**（新增"单对象操作排除产数值""多对象操作非空"）+ U0 52/52 + U1 35/35 回归；playwright 九步走查**零错误**，含**拖动进数值区**成功（`阶(e)` 落进数值面板）。
- 文档同步：INTERACTION §4.3/§4.4/§5/§9/§13.2 全部改成 v3 口径 + **新增 §13.3 KaTeX 适用范围** · ROADMAP 加 U2.5 小节 + 表格 ✅ · README（快速开始/状态/结构/换图）。
- **下一步 U3**：映射构建器（生成元填像）→ 第一条实线箭头；`ker`/`im` 自动可用。
- **UI v3 已提交并推送**（`578e3dd`，31 文件 +2090/−1522，远端 HEAD 已核对一致）。回归 U0 52 + U1 35 + U2 76 全绿后才提交；katex 依赖在 HEAD 里早已带上（此前装依赖时入库），本次无需动 package.json。
- **UI v3 已提交并推送**（`578e3dd`，31 文件 +2090/−1522，远端 HEAD 已核对一致）。回归 U0 52 + U1 35 + U2 76 全绿后才提交；katex 依赖在 HEAD 里早已带上（此前装依赖时入库），本次无需动 package.json。
