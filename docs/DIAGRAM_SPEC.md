# 交换图规范

> 这份文档回答两个问题：**课本级的交换图长什么样**，以及**因此什么对象适合上画布**。
>
> 结论来自 TeX 排版的权威资料——Paul Taylor 的 `diagrams` 包手册（交换图排版的经典文献）、
> `tikz-cd` 官方文档（当代事实标准）、AMS 的 `amscd`——以及数学期刊排版实践
> （MSP 的 *Fine-tuning a TikZ diagram*），并逐条对照 Galculator 的现状。

## 0. 交换图是什么（语法）

交换图只有两种成分：

| 成分 | 数学身份 | 图形身份 |
|---|---|---|
| **对象**（object）| 群、集合、模…… | **顶点** |
| **映射**（morphism）| 同态、同构、包含、投影…… | **边** |

「图交换」= **任取两条同起终点的路径，沿路复合出的映射相同**（方格：`β∘f = g∘α`）。
一个方格是一条方程，一个三角形是一条，一个立方体是六条。定理的"图"因此是**定理内容的等价改写**。

> 交换图不是装饰，是**压缩**：它把写出来要好几行的方程组压成一张图。
> 推论：**图上的每一笔都该在说"某条路径 = 另一条路径"**。
> 与此无关的内容画进去，就是在稀释它的信息密度。

## 1. 六条排版硬规范

### 1.1 对象落在格点上 ★最关键

`tikz-cd` 的核心设计就一句话（官方文档第 2.3 节附近）：

> "The arrows are annotations dropped into that skeleton afterwards, and
> **the positions of the objects are entirely unaffected by how the arrows are written**."

Taylor 说得更细——图被划分成**矩阵的格子**：

> "objects and verticals go in odd-numbered columns, and horizontals and diagonals go in even-numbered columns."

即：**先有格点，后有箭头**。箭头是往格点之间"挂"上去的注解。

**判据**：把图的行列延长成栅格，所有对象都应落在交点上（允许因宽度差异的微调，不允许"漂移"）。

> **✅ 已落地（2026-09-17 布局 / 2026-09-19 格点）**
>
> 布局是**硬约束网格**（实现见 [INTERACTION §8.1](INTERACTION.md)），而且**格点就画在画布上**：
> 它是一条**无限延伸的规则网格**——原点 + 固定步长，像坐标纸一样铺满整个画布，
> pan / zoom 到多远都有格点。两条路径都保证"落在格点上"：
>
> | 路径 | 怎么保证 |
> |---|---|
> | **自动布局** | 算出的行列中心**量化到最近的网格线**（顺带把不均匀的列宽差**拉平成等距**）|
> | **手动拖动** | 松手**吸附到最近的空格点**（跳过已被别的节点占住的格点）|
>
> 理由：这条规范如果只有系统知道、用户看不见，就退化成了"作者自己心里的网格"——
> 用户既不知道对象为什么停在那里，也不知道能把它放到哪。**格点画出来，规范才成立。**

### 1.2 箭头与对象中心对齐 ★

Taylor 开篇列的四条审美标准（原文）：

- Arrows should **stretch to meet** the objects which are intended to be their endpoints.
- Arrows should be **aligned (both horizontally and vertically) with the centres** of the objects.
- Labels on arrows should **not affect the spacing** of the diagram except to avoid overlapping.
- Stretching of arrows should **not affect the centering** of their labels.

第二条是"像不像课本插图"的**第一判据**：**水平箭头必须同高，垂直箭头必须同列。**
一条本该垂直的箭头只要歪几度，整张图立刻显得业余——哪怕其他一切都对。

### 1.3 箭头只允许少数几个方向

课本插图里出现的箭头方向只有三种：**水平、垂直、对角**。
对角箭头是"跨格点"的意思（`tikz-cd` 写作 `\arrow[rd]`），**方向是离散的**，不是任意斜率。

### 1.4 映射标签比对象小一号

MSP（专做数学期刊排版的机构）的实践说明：

> "Typically, map labels along arrows are in a **smaller font size** than the objects the maps go between."

`tikz-cd` 的默认也是 `\scriptstyle`（约 0.7 倍）。

> **✅ 已落地（2026-09-19）**：标签字号 = 两端对象字号的**较小者 × 0.68**（钳在 10–13），
> 于是集合节点（字号会自适应缩小）上的箭头标签也跟着缩，不会反客为主。

### 1.5 标签摆位只有一条规则：**沿行进方向的左侧**

`tikz-cd` 的 `auto` 规则：右向箭头的左侧 = 上方；下向箭头的左侧 = 左方。
不必为四个方向各记一条规则。加 `'`（单引号）翻到另一侧。

标签太长时可以**压在箭头上**，用背景色把线挖掉一段（`description` 样式）——
这比让标签去挤动布局好。

> **✅ 已落地（2026-09-19）**：屏幕坐标 y 向下，"方向 `(dx,dy)` 的左侧" = 法向 `(dy, −dx)`。
> 标签放在**沿箭头方向 30% 处**再往左让开 9（世界坐标）——30% 而不是中点，是因为
> 多条箭头汇聚到同一节点时中点会糊成一团（第二同构定理里有五条 `↪` 都指向 D₄）。
> 长标签挖空线由 `paintOrder="stroke"` + 白色描边实现。

### 1.6 箭头**形状**编码映射类型

| 形状 | 含义 | `tikz-cd` |
|---|---|---|
| 双箭头 | 满射 | `two heads` |
| 钩子 | 单射 | `hook` |
| **双向箭头** | **同构**（双射：同态 + 一一对应）| 课本里常写作 `\xrightarrow{\sim}` 或直接双头 |
| 虚线 | 存在唯一 | `dashed` |
| `≅` / `≃` 标在箭头上 | 同构（标注写法）| `"\simeq"` |

课本里 `π : G ↠ G/K`、`i : im φ ↪ H`、`G/K ≅ im φ` 三种箭头**一眼可分**，
因为它们分别承担定理的三个断言（满、单、双）。同构是**双射**，所以它的形状应当是**双向**的——
这一点 2026-09-19 按用户反馈改正（此前画成"双箭头 + 尾钩"，与满射、单射都撞脸）。

> **✅ 已落地（2026-09-19）**：`GalEdge.arrow`（`injective` / `surjective` / `iso`）由
> `derive.ts` 的 `arrowOf()` 从映射属性推出，画布把它翻成 marker：
> 满射 = `-surj`（双箭头，挂 `marker-end`）· 单射 = `-hook`（尾钩，挂 `marker-start`）·
> **同构 = `-head` 两端都挂**（`orient="auto-start-reverse"` 会把起点端翻向外侧，于是成双向箭头）。
> 结构伴生箭头也按**数学身份**给形状——`π` / `π₁` / `π₂` 是满射、`↪` 是单射、
> `≅`（第一同构的结论）与 `=`（轨道 = Ω，即"相等"）都是同构 → 双向。
> 映射属性判不出时（`null`）**不给形状**，不猜。
> 顺带修掉一个老瑕疵：marker 的颜色从前写死在定义里，于是蓝灰的伴生边配了个深色箭头；
> 现在 marker 按**视觉族**（prov / action / alongside / map）成套生成，箭头颜色必与线色同族。

## 2. 现状诊断

### 2.1 布局：列没有对齐（根因）

`apps/web/src/ui/CanvasView.tsx` 的布局分三步：

1. `computeLatticeLayout`（core）给出初始坐标
2. **按层归拢**：同层节点 y 取平均 → 水平方向对齐 ✓
3. **水平防重叠**：从左往右推 `pos[cur].x = minX` → **x 由"左边有几个节点、各自多宽"决定**

于是：**y 是对齐的，x 是推出来的。**
`G` 与 `G/ker φ` 落在不同列 → `π` 那条"垂直"箭头其实是斜的。
**这就是"能用但离课本级差得远"的直接原因。**

> **✅ 已修复（2026-09-17）**：改成**硬约束网格**——行 = 派生层（同层 y 严格相等），
> 列 = **竖直约束**的连通分量（只有 `π` / `π₁` / `π₂` / `↪` 这类结构伴生边要求同列；
> 用户造的映射是水平的、`≅` 是对角的，都不参与），列宽 = 该列最宽节点。
> 实测：第一同构定理自动排成课本的形状（`C₆ ──φ──▶ C₃` 水平、`π` 垂直、`≅` 对角）。
>
> **踩坑**：第一版按几何猜（"`dy ≥ dx` 的边要求同列"），结果 `≅` 这条对角边把
> `C₆`、`C₃`、`C₆/ker φ` 传染成一列，`C₆` 与 `C₃` 直接叠在一起。
> **判据必须按边的语义来，不能按几何猜。**

### 2.2 节点宽度参与布局（次因）

`measure()` 让节点宽度 = KaTeX 实测标签宽度 + padding。
`G`（窄）与 `G/ker φ`（宽）宽度不同 → 推挤时列宽参差 → 列与列之间没有稳定的栅格。

### 2.3 边没有方向约束

边连接任意两个节点中心，斜率任意。课本里只有水平 / 垂直 / 对角三种。

### 2.4 画布混装了"非交换图内容"

`Sub(G)`、`pSub`、元素集这类**列表型产物**也在画布上，靠**来源线**（虚线、可关、
语义是"谁由谁算出"）连起来。来源线不是数学关系，于是画布看起来像**依赖图**。

> **部分解决（2026-09-17）**：`ValueSort` 分层之后，`subgroups` 这类**列表**不再上画布
> （§3 的判据：能作为映射端点的才配当顶点），改去信息面板。

### 2.5 水平箭头从无关对象身上穿过（2026-09-19 修复）

A 把"竖直方向不许从对象身上穿过"写进了列约束（贪心合并 + 不许跨过同列节点），
但**水平方向没有对应的规则**。场景：画布上同时有 `C₆ ──φ──▶ C₃` 与另一个无关的
输入对象 `D₄`，两者同层（都是输入 → 排在同一行），`D₄` 恰好落在 φ 的两端之间
——**箭头从它身上穿过去**（真截图里抓到的）。

修法：**行内相邻约束**——同层的显式映射边，两端之间不许夹别的列组；夹着的组整体
挪到该行末尾，迭代至稳定。判据同样按**边的语义**：只有用户画的映射（带 `objectId`）算
水平关系，`π` / `↪` / `↷` 是竖直的、`≅` 是对角的，都不参与。

## 3. 什么对象适合上画布

判据直接来自 §0 的语法：**顶点必须是"对象"，边必须是"映射"**。

| 产物 | 是对象吗 | 上画布？ | 去处 |
|---|---|---|---|
| 群（`S₄` / `G/N` / `ker φ` / `im φ`） | ✓ | **是**（顶点）| 画布 |
| 集合（`Z(G)` / 共轭类等元素集） | ✓（集合之间也可以有映射）| **是**（顶点）| 画布 |
| 映射 / 同构 / 包含 / 投影 | ✓（作为边）| **是**（边）| 画布 |
| **子群集** `Sub(G)` / `pSub` / `Syl` | ✗ 是"对象的集合"，不是对象 | **否** | 信息面板（列表）|
| **元素 / 数值** | ✗ 是"点"，不是对象 | **否** | 元素表 / 数值区 |
| **作用** `共轭作用(G)` | ✓ 是 `G×Ω→Ω` 的关系——**第三种**，既不是顶点也不是边 | **是** | 作用线（与映射箭头视觉可分，见 §5.4 ③）|
| **Ω**（被作用的集合）| ✓ 集合 | **应当是顶点，但现在还不是对象** | 见 §5.4 ①：Ω 目前只是 `GalAction` 里的一个数字 |
| **来源线**（`A` 由 `B` 算出） | ✗ 计算过程，非数学结构 | 辅助 | 虚线，可关 |

**一句话准则**：

> **能作为某个映射的源或靶的，才配当顶点。**

`Sub(G)` 之所以不配：没有任何映射从它出发或指向它。它是个**列表**，
把它画成节点，等于把一张表格塞进交换图。

> **但列表不是死路。** 见 §5.4 ②：Sylow 定理要求 `Syl_p(G)` 这个列表
> **提升成集合 Ω**（取底集）之后才能被群作用。列表是**待提升的池子**。

**推论**：画布应当分成两层——**交换图区**（群 / 集合 / 映射 / 作用）
与**推演产物区**（子群集 / 元素集），后者的视觉权重必须明显低于前者。
（这一条是产品决策，见 §4 的 C。）

## 4. 改造路线

| 编号 | 内容 | 成本 | 收益 |
|---|---|---|---|
| **A** ✅ | **布局网格化**（2026-09-17 落地）：对象指派到整数格点，同列 x 严格相等、同行 y 严格相等；列宽 = 该列最宽节点 | 高 | ★★★ 直接决定"像不像课本" |
| **B** | **节点宽度与布局解耦**：格元尺寸固定，标签溢出不影响列宽 | 中 | ★★ 配合 A |
| **C** | **画布分层**：交换图内容与推演产物分区 / 分权重 | 中 | ★★★ 图立刻变干净 |
| **D** ✅ | **箭头形状编码映射类型**（2026-09-19 落地）：满射双箭头 / 单射钩子 / 同构两者兼有；顺带修掉 marker 配色错族 | 低 | ★★ 定理的三个断言一眼可分 |
| **E** ✅ | **标签摆位**（2026-09-19 落地）：沿行进方向左侧 + 标签小一号；长标签由白描边挖空线 | 低 | ★★ 细节 |

### A 的技术路线（硬约束，不是软约束）

网格布局的研究（*Incremental Grid-like Layout Using Soft and Hard Constraints*）结论明确：
**软约束**（"尽量对齐"）会得到 *nearly-but-not-quite aligned* 的结果——
节点看着差不多对齐、实际差几个像素，比明显不对齐更难看。
所以必须用**硬约束**保证行列严格对齐。

做法：

1. 以 core 的 lattice layout 为**初值**
2. 判断每条边的**主导方向**（水平 / 垂直 / 对角）
3. 施加硬约束：水平边两端统一 y；垂直边两端统一 x
4. 迭代至收敛 → 落到**整数格点**（col, row）
5. 按行高 / 列宽算最终坐标（列宽取该列最宽节点）

## 5. Sylow 定理的图长什么样

> 这是本项目 MVP 的目标，所以它的图形结构**直接决定画布该支持什么**。

### 5.1 它不是交换图，是「作用图」

Sylow 三个定理的证明骨架是**同一个模式**：

```
造一个集合 Ω  →  让群作用在它上面  →  分解成轨道  →  数数
```

| 定理 | Ω（被作用的集合）| 作用 | 结论从哪来 |
|---|---|---|---|
| **Sylow I**（Wielandt）| `p^k` 元子集全体 | G 在子集上的自然作用 | 轨道分解 + 计数 mod p |
| **Sylow II** | `G/P` 的左陪集 | H 的左乘作用 | 不动点集非空 ⟹ 存在共轭 |
| **Sylow III** | `Syl_p(G)` | G 的共轭作用 | **只有一个轨道**（由 Sylow II）→ 轨道-稳定子 |

Clemson 的 Macauley 讲义（群作用教学的通用参考）里有一句点明图示约定的话：

> "**the orbits of φ are the connected components in the action diagram**"
> ——轨道就是作用图的连通分量。

### 5.2 三层结构

以 Sylow III 为代表，图是**三层**，而不是一排并列的对象：

```
        φ
  G ─────────▶ Perm(S)          ← 上层：同态（唯一像交换图的部分）
  │
  │ ↷ 共轭作用
  ▼
  S = Syl_p(G)                  ← 中层：Ω，被作用的集合
  │
  │ 轨道分解
  ▼
  Orb(H) = S（唯一）             ← 下层：轨道（Ω 的子集）
  │
  │ 稳定子
  ▼
  Stab(H) = N_G(H) ↪ G           ← 下层：稳定子（G 的子群）

  |Orb(H)| · |Stab(H)| = |G|  ⟹  n_p · |N_G(H)| = |G|
```

**注意上层的箭头与中层的箭头是两种不同的东西**：
`φ: G → Perm(S)` 是**同态**（贴着交换图语法），而 `G ↷ S` 是**作用**（`G×S → S`）。
课本把它们叠在一张图里，但它们不是同一类关系。

### 5.3 轨道分解是「切块」，不是一串独立节点

作用把 Ω 切成若干块，**块的大小 = 轨道长度**。固定点就是长度 1 的块。

```
Ω ┌──────┬─────────────┬──────────────────┐
  │  1   │      3      │        4         │
  └──────┴─────────────┴──────────────────┘
   Fix      O₂              O₃

|Ω| = |Fix| + Σ|Oᵢ|        p-群作用 ⇒ |Ω| ≡ |Fix| (mod p)
```

视觉上这是**一个集合的「内部分割」**，而不是三个并列的对象。
（对应 INTERACTION §2 已有的「数学浓缩（复合节点）」概念。）

### 5.4 对对象栈的启示

| # | 启示 | 现状 |
|---|---|---|
| ① | **Ω 必须是一等对象** | ✅ **已做（2026-09-17）**：`GalAction.omega` + derive 找 / 造 Ω 节点 |
| ② | **需要 `list → set` 提升**：`Syl_p(G)` 是列表（按 §3 不上画布），但必须能"取底集"成集合 Ω，才能被作用 | **缺失**。这条正好接上 §3 的结论：**列表不是死路，是待提升的池子** |
| ③ | **作用不是装饰，是主角** | ✅ **已做**：`sortOf(action) = 'edge'`，画布上是 `G ↷ Ω` 的**一等边**（不再占节点）；视觉上与映射可分（橙 `↷`）|
| ④ | **轨道分解 = 结构伴生**，且同一作用的所有轨道应**一起出现** | 部分：轨道现在是 Ω 的**子集**（`set` 值）且带 `Orb ↪ Ω`（等于 Ω 时写 `=`）的边；**「一起出现」（Ω 展开成切块）仍未做** |
| ⑤ | **数值该贴在对象旁**：`n_p` 是**轨道的长度** | 部分：`n_p` 现在出现在**作用对象的结论区**（`n_p ≡ 1 (mod p)`、`n_p = [G : N_G(H)]`），不再只是孤立数字；但还没「贴到轨道节点旁」 |

### 5.5 对操作栈的启示：三定理共用一条流水线

```
造集合 → 作用 → 轨道 / 稳定子 → 数值
```

三个定理只是换了 Ω 与作用方式。这条链应当作为**可复用的配方**存在
（ARCHITECTURE §9 的"配方是元数据"），而不是三份各写一遍的操作序列。

## 6. 交换图与作用图如何平衡

> 这是 §5 之后必然要回答的问题：既然 Sylow 的图是"作用图"，
> 那它跟交换图语法不同，画布到底该按哪个来？

### 6.1 先澄清一个容易搞混的地方

"作用图"这个词指的是**两种不同的东西**：

| | **对象级**（证明用的）| **元素级**（举例用的）|
|---|---|---|
| 顶点 | **对象**：群、集合、子群 | **Ω 的元素**（点）|
| 边 | 映射 `G → H`、包含 `H ↪ G`、作用 `G ↷ Ω` | "被 g 搬运"`pt → pt` |
| 核心断言 | 路径无关（方格交换）| 连通性（**轨道 = 连通分量**）|
| 教材里出现于 | **Sylow 三定理的证明陈述** | **讲解"轨道是什么"时的举例** |

Macauley 那句 "orbits are the connected components in the action diagram"，
说的是**下排**；但 **Sylow 定理本身的那几张图全在上排**——
`G`、`Syl_p(G)`、`Orb(H)`、`N_G(H)`，**每一个顶点都是对象**。

**结论：Sylow 的图与第一同构定理的图处在同一层，用的是同一套语法。**
差别只是多了三种元件：

- 一种新**顶点**：集合 `Ω`（现在还不是对象）
- 一种新**边**：作用 `G ↷ Ω`（现在被归成辅助）
- 一种新**内部结构**：轨道分解（`Ω` 的切块）

### 6.2 平衡方案：一种语法，三层密度

```
第 1 层 · 对象级（默认视图）
   对象 + 关系（映射 / 包含 / 作用）
   → 交换图与作用图在这一层完全统一

第 2 层 · 集合内部（展开一个集合节点时）
   轨道分解的切块：块的大小 = 轨道长度
   → Sylow III 里只有一个块（⇒ n_p = |S|）
   → Sylow I 里要数这个划分（|Ω| ≡ |Fix| mod p）

第 3 层 · 元素级（很少需要）
   "g 怎么搬运点" —— 不进画布，去信息面板的横滚表格
```

**默认停在第 1 层**。需要时才对某个集合节点下钻一层。
这套"渐进展开"不是新机制——它就是 INTERACTION §2 早就写的
「数学浓缩 / 视觉折叠」，只是现在有了明确的**下钻顺序**。

### 6.3 一句话判据

> **画布画「对象之间的关系」；面板画「元素之间的关系」。**

| 图 | 第 1 层 | 第 2 层 | 需要第 3 层吗 |
|---|---|---|---|
| 第一同构定理 | `G → H`、`G/ker φ`、`im φ` + 三条边 | — | 不需要 |
| Sylow III | `G ↷ S = Syl_p(G)` | 展开 `S`：**一个**轨道块 | 不需要 |
| Sylow I | `G ↷ X`（`p^k` 元子集）| 展开 `X`：数块大小 | 不需要 |
| 讲解"轨道是什么" | — | — | **需要**（Cayley 式，通常放面板）|

### 6.4 实现要求（三条）—— ①② 已落地（2026-09-17）

1. ✅ **`Ω` 升格为对象**：`GalAction` 带上 `omega: GalSet` + `omegaBase: 'self' | 'object'`；
   `derive` 为它**找家**（用户那行 `Ω = 底集(S)` 的节点）或**就地造一个**（内联写法）。
   `Ω = G` 自身时（共轭 / 正则作用）作用线画成 **G 上的自环**。
2. ✅ **作用成为一等边**：`action` 的 `canvasShape` 从 `'action'`（占节点）改成 `'edge'`；
   边带 `objectId` → **可点选**（点 `G ↷ Ω` 就选中那个作用，环绕直接铺出 `轨道` / `稳定子` / `不动点`）。
   轨道边 `Orb ↪ Ω`（等于 Ω 时标签写 `=` —— 那就是传递性），稳定子边 `Stab ↪ G`。
3. ⏳ **集合节点可展开**：展开后显示轨道分解的切块（第 2 层）——**未做**。

①②做完之后，`第一同构定理` 与 `Sylow III` 已经能画在**同一张画布**上，画布只有一种语法。

## 7. 定理复现体检（2026-09-17）

> **方法**：到网上取**教材里真实存在的交换图**，逐条尝试用本项目复现。复现不出来就是缺口。
> 脚本：`apps/web/.tmp-verify/verify-theorems.ts`（vite SSR 打包后 node 直跑）。

### 7.1 结果总览

| 定理 | 表达 | 计算 | 出图 | 结论 |
|---|---|---|---|---|
| 第一同构定理（三角形）| ✅ | ✅ | ✅ | **完全复现** |
| 第一同构定理（含 `im φ` 的正方形）| ✅ | ✅ | ✅ | 复现（`im φ` 要手写一行）|
| **第二同构定理**（菱形 + 两侧商群）| ✅ | ✅ | ✅ | **完全复现** |
| 第三同构定理（梯形）| ⚠️ | ⚠️ | ⚠️ | 卡在 G4 |
| 短正合列 | ✅ | ✅ | ✅ | **形状障碍已清除**（2026-09-19，G6 解决）：`↪` / `↠` 画得出来了（尚未实跑复现）|
| 轨道-稳定子定理 | ✅ | ✅ | ✅ | 完整复现 |
| **Sylow 第三定理** | ✅ | ✅ | ✅ | **完整复现（2026-09-17，G5 / G7 修掉后）** |

### 7.2 实测通过：第二同构定理

```
G = D_4
H = 闭包(G, r)
N = 闭包(G, r2, s)
HN = 闭包(G, r, s)
I = H ∩ N
Q1 = H / I
Q2 = HN / N
```

七行全部求值成功，阶**逐一与理论值相符**：

| 对象 | 实测 | 理论 |
|---|---|---|
| `H = ⟨r⟩` | 4 | 4 |
| `N = ⟨r², s⟩` | 4 | 4 |
| `HN` | 8 | = G |
| `H ∩ N` | 2 | `⟨r²⟩` |
| `H/(H∩N)` | 2 | 4 / 2 |
| `HN/N` | 2 | 8 / 4 |

画布长出的边：`H ↪ G` · `N ↪ G` · `HN ↪ G` · **`H∩N ↪ H`** · `H →(π) H/(H∩N)` · `HN →(π) HN/N`
—— 菱形的三条包含边 + 两侧的商投影，都在。

### 7.3 体检修掉的三个缺口

| # | 缺口 | 症状 | 修法 |
|---|---|---|---|
| **G1** | **元素记号不认数学惯例** | `闭包(G, r4)` 报"没有元素 r4"。core 的 `C_n` 是**加法群**（生成元叫 `a`、元素是 `0..n-1`），课本写的是乘法循环群 `r^k` | 新增 `resolveElementLoose` 三级回退：core 精确 → **生成元的幂**（`r4` / `r^4` / `r^{4}`）→ **单生成元群的单字母别名**（`r`/`a`/`g` 互通）|
| **G3** | **子群之交不升级为群** | `H∩N` 是 `elements` ⇒ 画布上是个"集合圆"，而且**没有 `↪` 包含箭头**（只有虚线来源线）| `setOp` 在 `∩` / `·` 且结果**确实是子群**时升级为真群对象；`derive` 把 `intersection` / `productSet` 加进 `SUBGROUP_RESULT_OPS` |
| **G3b** | **`闭包(J)` 得到平凡群** | `Z ∩ C` 阶 2，`闭包(Z∩C)` 却阶 1 | `闭包` 的"上下文群"形态**只在后面还有元素参数时**才成立；单个群参数走"取它的元素当种子" |

> **G3b 是体检挖出来的老 bug**（U0 就埋着）：`闭包(G, r2)` 与 `闭包(J)` 的歧义。
> 它此前从未被测到——既有断言只覆盖了 `闭包(G, r2)` 形态。

> **G3 与决策 ⑤ 的关系**：决策 ⑤ 说"固化集合不自动升级"，那针对的是**用户手工构造的集合**
> （`{x ∈ G : φ(x)}`，是不是子群要判断）。而**两个子群之交必是子群——这是定理**，
> 无需判断，所以直接升级。

### 7.4 仍然复现不出来的（缺口清单）

| # | 缺口 | 挡住的图 | 严重度 |
|---|---|---|---|
| **G4** | **商群的子群判定失败** | 第三同构定理 `(G/N)/(K/N)`：报"KN 不是 GN 的子群"——两个商群的元素是各自 `computeQuotientGroup` 造的，**id 体系不同** | 高 |
| ~~**G5**~~ | ~~共轭作用在子群集上缺失~~ | ✅ **已解决（2026-09-17）** | — |
| ~~**G6**~~ | ~~箭头形状不编码映射类型~~ | ✅ **已解决（2026-09-19）**：`GalEdge.arrow`（单射 / 满射 / 同构）由映射属性推出（`arrowOf`），画布翻成双箭头 / 尾钩 / 两者兼有 | — |
| ~~**G7**~~ | ~~作用仍占节点、轨道/稳定子的边是虚线~~ | ✅ **已解决（2026-09-17）** | — |
| **G8** | **不自动补 `im φ`** | 第一同构定理的正方形版要手写 `I = im(ψ)`（工具只补 `G/ker φ`）| 低 |

### 7.4b 一条更正：core 其实**有**现成的

§7.4 初版把 G5 写成「core 无现成函数，要自己实现」，这是**错的**——翻过 `algebra/sylow.d.ts` 之后发现 core 早就给了两个：

| 函数 | 作用 |
|---|---|
| `conjugateSubgroup(group, elements, g)` | `gHg⁻¹`（返回**排序后**的元素数组，所以可以按 id 排序做键）|
| `sylowConjugationPerms(group, subgroups)` | **就是** G 在 Sylow p-子群全体的共轭作用（返回 `Map<元素id, 置换>`）|

本轮实现走的是 `conjugateSubgroup` 那条（更一般：任何共轭闭的子群族都能作用，不只是 Sylow），
并在断言里与 `sylowConjugationPerms` **逐行交叉核对**过。

> 教训：写「core 没有 X」之前，**先 `grep` 一遍 `.d.ts`**。
> 「凭印象宣布库不支持」的代价是白写一遍。

### 7.5 结论

**七条里四条能完整复现，两条卡在缺口上，一条部分可用。**
（2026-09-19 起：G5 / G7 / G6 已解决，剩 G4 与 G8。）

体检还证明了一件事：**"算得对"与"画得对"是两件事**。
第二同构定理在 G3 修复**之前**，阶**全对**（`|H/(H∩N)| = |HN/N| = 2`），
但图上 `H∩N` 是个"集合圆"、两条包含箭头是虚线 —— **数学对、图不对**。
这正是 §3 那条判据（"能作为映射的源或靶的，才配当顶点"）在实施层面的价值。

六条硬规范（§1）到 2026-09-19 全部落地：①格点 ②箭头与中心对齐 ③方向只有三种
（前三由 A 的硬约束网格保证）④标签小一号 ⑤标签沿行进方向左侧 ⑥箭头形状编码类型。

## 参考

- Paul Taylor, *Commutative Diagrams in TeX*（`diagrams` 包手册）——§4 的审美标准原文
- `tikz-cd` 官方文档（CTAN，v1.0 / 2021）——对象-箭头的"格点 + 注解"模型、标签规则、线型语义
- `amscd`（AMS）——矩形网格的最小实现
- MSP, *Fine-tuning a TikZ diagram*——映射标签的字号规范
- Kieler / ar5iv, *Incremental Grid-like Layout Using Soft and Hard Constraints*——硬约束的必要性
- Wikipedia, *Graph drawing*——可量化的美学准则（交叉最少、边长均匀、对称）
