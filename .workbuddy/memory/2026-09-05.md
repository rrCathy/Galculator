# 2026-09-05

## 新项目启动：群论计算器（Galculator 目录）
- 定位：群论的交互式"计算 + 证明可见化"工具，对标 Desmos/GeoGebra 成熟度（市面空白）。
- 部署：先做网页，成熟后再做 app（参考 desmos 双端）。
- 计算栈：已定 Sage/GAP 后端（否决 SymPy 主力——缺小群库/结构识别/同构判定）。
- 可视化：联动 GroupViz；GroupViz 正在"引擎化"，不久发包 → 前端直接 import 引擎，不再 iframe。
- 两份契约：Group Spec JSON（群描述）+ Proof Spec（证明模板），需与 GroupViz 引擎输入对齐。

## MVP 定位（关键转向）
- 用户否决"输入群→识别→可视化"作为 MVP（太简单）。
- 新 MVP：**通过群作用证明 Sylow 定理**——产品从"计算器"抬为"证明可见化器"。
- 已澄清边界：演示性证明（模板人工写，机器在具体群上实例化+可视化），非形式化证明（Lean/Coq 级，不碰）。
- 群作用（G ↷ Ω）升为一级对象：轨道/稳定子/传递性/忠实性/orbit-stabilizer。
- Proof Spec = 步骤序列（声明 | 计算调用）+ 展示方式；Sylow 是第一个模板实例，后续可挂 Cayley 定理、第一同构定理等。
- 待用户拍板：①演示性确认 ②Sylow 只做 I 还是 I+II+III ③授权去 GroupViz 读引擎化代码对齐 schema。

## 已拍板 + GroupViz 调研结论（20:06）
- 已拍板：演示性证明 ✓；Sylow I/II/III = 三份 Proof Spec 模板，全做（用户："三个定理就是三份模板的事"）；授权读 GroupViz ✓。

## GroupViz 引擎化现状（已读源码）
- **GroupDescriptor v1 已实现**（src/core/descriptor.ts，zod）：schemaVersion/symbol/name/order/elements[{id,label,order}]/multiply(乘法表行序)/properties/construction/source。→ 计算器的"群描述"直接对齐它，不重造。
- **双包已定案**（ROADMAP §2.8）：@groupviz/core（纯算法零 React）+ @groupviz/react（视图），2026-10 启动，现处阶段1（序列化固化）。core 预留 createBackendAdapter({baseUrl,fetchImpl})。
- **GroupViz 已有 GAP 4.16 后端**（backend/gap_service.py + import-group + 六端点 >120 阶切 GAP）。
- **Sylow 已算结果未算证明**：sylow.ts computeSylowAnalysis 给 np/子群/np≡1 mod p/np|m；群作用五源 actions.ts（共轭/正则/陪集/自定义/sylow，含 orbit/stabilizer/OST）。
- 引擎门面 src/core/index.ts（FGVE 协议雏形）；Group 核心类型 types/group.ts。

## 对接四结论
1. 群描述 = GroupDescriptor v1，不重造 Group Spec。
2. 护城河 = 证明层（Proof Spec 组织 Wielandt 步骤），非"算"。
3. 后端扩展 GroupViz 的 GAP（复用 gap_service + 加证明步骤端点），不另起 Sage。
4. Proof Spec 执行器放前端（step-through 交互密集），后端只返回原子计算 JSON。

## 待用户拍板（下一轮）
- ①时间线错位：A 先对齐协议+复用现有 src/core 做 Proof Spec 原型，等包切 @groupviz/*（推荐）| B 等包。
- ②后端：扩展 GroupViz GAP（推荐）| 独立 Sage。

## 已拍板 + 文档骨架建立（20:32）
- 已拍板：时间线选 A（不等包，先用 GroupViz src/core 开工）；后端"能算就行选方便的"= 扩展 GroupViz GAP；Proof Spec schema 与粒度 C 均"觉得还行"。
- Proof Spec 草案定稿：三种步骤（claim/compute/conclude）+ 双 target（local GroupViz 原语 / backend GAP 端点）+ ShowSpec（symbol/diagram/table）。
- Sylow I Wielandt 完整 12 步实例（A₄, p=2）已定；粒度 C（计数存在性 + GAP 实例化，不循环）。
- 开发规划 M0-M4 定案：M0 最小闭环 / M1 Proof Spec 引擎 / M2 Sylow 三定理 / M3 定理库+交互 / M4 发布+教育。
- 技术栈：前端 React19+TS+Vite；后端扩展 GroupViz GAP；复用 GroupViz src/core（vite alias→后切 @groupviz/core）；KaTeX。
- 文档骨架已建：README.md / docs/DESIGN.md / docs/ROADMAP.md / docs/PROOF_SPEC.md（均为大纲+要点，细节待补）。
- 下一步：按用户节奏补细节 → 启动 M0 最小闭环。

## GroupViz 对接待办调研（20:40，现场核源码）
- 目标：回答"GroupViz 需要做什么以方便对接"。证据全部来自现场 grep/read。
- **P0 阻塞①**：descriptor.ts（GroupDescriptorV1 schema + serialize/deserialize）**未从 index.ts/types.ts 门面导出**——协议是"内部孤儿"，Galculator 用不了（除非违规深导入）。
- **P0 阻塞②**：src/core 越界引用：notationParser.ts→../../utils/groupFactory、importGroup.ts→../../utils/api；而 utils/ 夹带 katex/three/zod/gifenc 重型依赖。→ src/core 非纯算法层，vite alias 会拖进这些。
- **P1 协议**：factorizeOrder 已在 sylow.ts ✅；binomial(组合数 C(n,k) mod p) **缺失** ❌（Wielandt 计数段需要）；GroupDescriptor v1 无对外文档/版本策略；缺"稳定公共原语表"（Proof Spec local op 白名单）。
- **P2 后端**：main.py 有 14 端点（health/group-info/subgroups/normal-subgroups/conjugacy-classes/center/cosets/lattice/cayley-edges/element-order/direct-product/properties/series/import-group），**缺轨道分解/稳定子/orbit-stabilizer 证明级端点**。
- **P3 视图**：Proof Spec 需 action 轨道图 + 陪集分解图 + highlight 能力，需确认 GroupViz 是否已有。
- **P4 长期**：拆包 @groupviz/core+react（ROADMAP §2.8，不阻塞）。
- 建议先做 P0 两件（启动 M0 的前提）。
- 已产出交接 prompt 存档：docs/GROUPVIZ_HANDOFF.md（自包含，含背景/证据/任务/验收标准，供用户发给 GroupViz agent）。
